# Frontend Environment Variables
# Copy this to .env (in THIS directory: apps/zerg/frontend-web/.env)
# for local development outside Docker

# NOTE: vite.config.ts loads .env from apps/zerg/frontend-web/ directory
# Docker Compose sets these via docker-compose.dev.yml instead

# ============================================================================
# Local Development (without Docker Compose)
# ============================================================================

# Proxy target for Vite dev server
# Use this when running frontend locally but backend in Docker or remote server
# Default: http://backend:8000 (leverages Docker Compose DNS)
VITE_PROXY_TARGET=http://localhost:47300

# Frontend dev server port
FRONTEND_PORT=47200

# ============================================================================
# Browser Configuration (VITE_ prefix required)
# ============================================================================

# API base URL for client-side requests
# In development with proxy: /api (proxied to backend)
# In production: full URL like https://api.example.com
VITE_API_BASE_URL=/api

# WebSocket base URL for real-time connections
# In development: ws://localhost:47300/ws
# In production: wss://api.example.com/ws
VITE_WS_BASE_URL=ws://localhost:47300/ws

# Google OAuth Client ID (optional for authentication)
# VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# ============================================================================
# Feature Flags (optional)
# ============================================================================

# VITE_AUTH_ENABLED=true
# VITE_ENABLE_PERFORMANCE=false
# VITE_ENABLE_MEMORY_MONITORING=false
# VITE_ENABLE_ERROR_REPORTING=false

# ============================================================================
# Notes
# ============================================================================
#
# - Only variables prefixed with VITE_ are exposed to client code
# - In Docker Compose, these are set in docker-compose.dev.yml
# - For local laptop dev, copy this file to .env and customize
# - VITE_PROXY_TARGET is only needed when running frontend outside Docker
