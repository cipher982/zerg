# ==============================================================================
# SWARM PLATFORM ENVIRONMENT VARIABLES
# Unified configuration for Jarvis (voice/text UI) + Zerg (agent backend)
# ==============================================================================
# Copy to `.env` and adjust values for your environment

# ------------------------------------------------------------------------------
# JARVIS (Voice/Text Interface)
# ------------------------------------------------------------------------------
# OpenAI API key for Jarvis Realtime API and MCP tools
JARVIS_OPENAI_API_KEY="sk-your-key-here"

# Jarvis server ports
JARVIS_SERVER_PORT="8787"
JARVIS_WEB_PORT="8080"

# Device secret for Jarvis authentication with Zerg backend
JARVIS_DEVICE_SECRET="jarvis-secure-device-secret-change-me"

# Zerg backend URL for Jarvis to connect to
JARVIS_ZERG_API_URL="http://localhost:47300"

# ------------------------------------------------------------------------------
# ZERG (Agent Orchestration Backend)
# ------------------------------------------------------------------------------
# Database connection
DATABASE_URL="sqlite:///./app.db"
# For production PostgreSQL: postgresql://user:pass@host:port/dbname

# Zerg server ports
BACKEND_PORT="47300"
FRONTEND_PORT="47200"

# OpenAI API key for Zerg agent execution
OPENAI_API_KEY="sk-your-key-here"

# Optional: cap LLM completion size (0 disables)
MAX_OUTPUT_TOKENS="1000"

# Optional: per-user daily runs cap (0 disables)
DAILY_RUNS_PER_USER="100"

# Optional: restrict non-admin users to specific models
ALLOWED_MODELS_NON_ADMIN="gpt-4o-mini"

# ------------------------------------------------------------------------------
# Authentication (Google OIDC for Zerg UI)
# ------------------------------------------------------------------------------
GOOGLE_CLIENT_ID="change-me.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="change-me-secret"

# Secret used to sign application-issued JWT access tokens (HS256)
JWT_SECRET="dev-secret"

# Development shortcut: when set to "1" all auth checks are bypassed
AUTH_DISABLED="1"

# Public URL used to construct callback URLs
APP_PUBLIC_URL="https://your-ngrok-url.ngrok.io"

# ------------------------------------------------------------------------------
# User limits / Admin
# ------------------------------------------------------------------------------
MAX_USERS="20"
ADMIN_EMAILS="me@example.com"

# Database reset security password (production only)
DB_RESET_PASSWORD="your-secure-reset-password-here"

# ------------------------------------------------------------------------------
# Notifications (Discord)
# ------------------------------------------------------------------------------
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/your-webhook-id/token"
DISCORD_ENABLE_ALERTS="0"
DISCORD_DAILY_DIGEST_CRON="0 8 * * *"

# ------------------------------------------------------------------------------
# Gmail Pub/Sub (Production)
# ------------------------------------------------------------------------------
GMAIL_PUBSUB_TOPIC=""
PUBSUB_AUDIENCE=""

# ------------------------------------------------------------------------------
# Environment
# ------------------------------------------------------------------------------
ENVIRONMENT="development"
